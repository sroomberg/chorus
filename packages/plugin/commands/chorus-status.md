---
description: Show chorus sharing/join state, pending join queue, and connected users.
---

Use the chorus-status tool and display the results clearly.
If pendingQueueText is present, print that numbered join queue so the host can /chorus-approve <id> or /chorus-deny <id> without a full userId.
