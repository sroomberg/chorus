---
description: Share the current session. Usage: /chorus-share [edit|view|admin]
---

Use the chorus-share tool with role "$ARGUMENTS". If no role argument is given, use "edit".
Print the returned token and host address clearly so the user can share them.
