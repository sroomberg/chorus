---
description: Join a shared chorus session. Usage: /chorus-join <token> <host> [name]
---

Parse the arguments "$ARGUMENTS" as: first word = token, second word = host (e.g. 192.168.1.5:7742), remaining words = display name (optional).
Use the chorus-join tool with those values.
